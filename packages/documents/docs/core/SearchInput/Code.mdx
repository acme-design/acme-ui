import { Playground, Props } from '~controls';
import { SearchInput } from '@acme-ui/core';

# 基本用法

<Playground>
  {() => {
    const onSearch = (value) => {
      console.log('value: ', value);
    };
    return <SearchInput clear onSearch={onSearch} />;
  }}
</Playground>

# 基本用法

<Playground>
  {() => {
    const onSearch = (value) => {
      console.log('value: ', value);
    };
    return <SearchInput clear onSearch={onSearch} />;
  }}
</Playground>
